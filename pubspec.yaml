name: yab_v2
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.15+19

environment:
  sdk: ">=2.18.0-36.0.dev <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.5
  logger: ^1.1.0 # 로그 라이브러리
  get: ^4.6.5 # 상태관리 라이브러리
  firebase_core: ^1.20.0 # 파이어베이스 라이브러리
  firebase_auth: ^3.9.0 # 파이어베이스 라이브러리
  firebase_storage: ^10.3.4 # 파이어베이스 파일 저장 라이브러리
  firebase_messaging: ^12.0.1 # 파이어베이스 푸시 알림 라이브러리
  cloud_firestore: ^3.4.3 # 파이어베이스 라이브러리
  geoflutterfire: ^3.0.3 # GPS 라이브러리
  google_sign_in: ^5.4.1 # 구글 로그인 라이브러리
  animated_splash_screen: ^1.3.0 # SplashScreen제작에 필요한 라이브러리
  flutter_spinkit: ^5.1.0 # 로딩페이지에 들어가는 그림 라이브러리
  extended_image: ^6.2.1 # 이미지(네트워크, 로컬) 가져오기 관련 라이브러리
  image_picker: ^0.8.5+3 # 스마트폰에서 사진을 가지고 올수 있는 라이브러리
  introduction_screen: ^3.0.2 # introPage에서 사용하는 슬라이드 안내 페이지 라이브러리
  flutter_local_notifications: ^9.7.0 # 푸시 알림 관련 라이브러리
  http: ^0.13.5 # 푸시 알림 보내기 관련 라이브러리
  intl: ^0.17.0 # 숫자 통화 1,000,000 과 같이 표시하는 라이브러리
  auto_size_text: ^3.0.0 # 글자크기 자동 수정 및 줄바꿈 라이브러리
  flutter_launcher_icons: ^0.10.0 # 앱아이콘 추가를 위한 라이브러리
  google_fonts: ^3.0.1 # 구글 폰트 라이브러리
  table_calendar: ^3.0.6 # 출석체크를 위한 캘린더 라이브러리
  flutter_markdown: ^0.6.10+3 #markdown 문법(md파일 약관 읽는 라이브러리)
  sign_in_with_apple: ^4.1.0 # 애플 로그인 라이브러리 (13버전 이상)
  flutter_web_auth: ^0.4.2 # 애플 로그인 라이브러리 (13버전 미만)
  uuid: ^3.0.6 # 애플 로그인 라이브러리 (13버전 미만)
  google_mobile_ads: ^2.0.0 # Admob 관련 라이브러리
  shared_preferences: ^2.0.15 # 내부 저장소를 이용하는 라이브러리
  firebase_analytics: ^9.3.0 # 파이어베이스 아날라이틱스 유저의 행동 패턴 분석 라이브러리
  dio: ^5.1.1 # API (주소) 호출 이용 라이브러리
  flutter_datetime_picker: ^1.5.1 # 날짜, 시간 선택기 원통형
  numberpicker: ^2.1.1 # 숫자 선택기 원통형
  upgrader: ^4.5.0 # Android & IOS 버전체크하는 라이브러리
  package_info_plus: ^1.4.3 # Android & IOS 패키지 정보 가져오는 라이브러리
  flutter_email_sender: ^5.1.0 # 이메일 라이브러리(문의하기)
  qr_flutter: ^4.0.0 # QR Code 만드는 라이브러리
  qr_code_scanner: ^1.0.0 # QR Code 읽는 라이브러리
  badges: ^2.0.3 # 알림 뱃지 라이브러리
  encrypt: ^5.0.1 # QR코드 문자열 암호화&복호화 라이브러리
  crypto: ^3.0.2 # QR코드 문자열 암호화&복호화 라이브러리
  cloud_functions: ^3.3.3 # Cloud Functions 사용 라이브러리
  photo_view: ^0.14.0 # 사진 라이브러리
  decimal: ^2.3.0 # 십진수 라이브러리 (계산정확도 필수)

dependency_overrides:
  firebase_core_platform_interface: 4.5.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo/app-icon.png"

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/icons/
    - assets/images/logo/
    - assets/images/shop/
    - assets/images/event/
    - assets/document/
    - assets/images/entertainment/
    - assets/ring/yab.mp3
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:

  #fonts:
  #  - family: DoHyeon
  #    fonts:
  #      - asset: assets/fonts/BMDOHYEON_otf.otf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
